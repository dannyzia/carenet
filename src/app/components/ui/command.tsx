// Reverse bridge — real code lives in /src/frontend/components/ui/command.tsx
export { Command, CommandDialog, CommandInput, CommandList, CommandEmpty, CommandGroup, CommandItem, CommandShortcut, CommandSeparator } from "@/frontend/components/ui/command";
